database:
    mysql:
        host: localhost
        user: mysql
        password: mypassword
    postgresql:
        name: postgre
        host: localhost
        user: psql
        password: mypassword